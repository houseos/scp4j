package de.delusionsoftware.shuttercontroller.network.shutterControlProtocol;

public class SecurityMessageFactory {

//	public String createSecurityPasswordChange(final Device device, final String newPassword) {
////		final StringBuilder url = new StringBuilder();
////		url.append("http://");
////		url.append(device.getIpAddress());
////		url.append("/shutter-control?payload=");
////
////		// encrypt device id with password
////		final AES128CBC cryptEngine = new AES128CBC();
////		final Encoder encoder = new Encoder();
////		System.out.println("CipherText: " + encoder.base64thenURL(cryptEngine
////				.encrypt(device.getDeviceID() + ":security-pw-change:" + newPassword, device.getPassword(), iv)));
////		url.append(encoder
////				.base64thenURL(
////						cryptEngine.encrypt(device.getDeviceID() + ":security-pw-change:", device.getPassword(), iv))
////				.trim());
////		System.out.println("URL: " + url.toString());
////		return url.toString();
//	}
}
